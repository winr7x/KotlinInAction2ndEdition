package ch02.colors

enum class Color {
    RED, ORANGE, YELLOW, GREEN, BLUE, INDIGO, VIOLET
}

// 'enum' is a so-called soft keyword: it has a special meaning when it comes before class,
// but you can use it as a regular name
fun enum() { // 'enum' as name of function
}